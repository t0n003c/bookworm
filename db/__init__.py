"""db — database schema + migrations (consumed by core.db.get_db()).

Split out of the old monolithic database.py (see ARCHITECTURE.md, Phase 2b).
database.py re-exports these for backward compatibility.
"""
