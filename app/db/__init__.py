"""app.db — database schema + migrations (consumed by app.core.db.get_db()).

schema  — the full current schema (CREATE TABLE statements) + seed defaults
migrations — init_db(): additive/idempotent migrations run on every boot
"""
