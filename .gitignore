# ── Secrets & local config — NEVER commit ──────────────────────────────
.env
*.secret
*_cookies.txt
_qa*.txt

# ── Databases & user-uploaded data ─────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.db-journal
uploads/

# ── Logs ───────────────────────────────────────────────────────────────
*.log

# ── Generated ML artifacts (rebuilt at runtime by search_index.py) ─────
bw_tfidf_*.pkl

# ── Python ─────────────────────────────────────────────────────────────
__pycache__/
*.pyc
.venv/

# ── Claude Code: keep shared agents, drop local-only settings ──────────
.claude/settings.local.json

# ── Windows-only Tailwind binary (40MB; CSS is pre-built/bundled) ──────
tailwindcss.exe

# ── Scratch / build outputs ────────────────────────────────────────────
openapi_tmp.json
audit_out.txt
eqp_out.txt

# ── OS / editor cruft ──────────────────────────────────────────────────
.DS_Store
